elasticsearchPUBLICBASEURL: https://logs.sirius.online/
elasticsearchHosts: http://10.43.225.153:9200
elasticsearchURL: http://elasticsearch-master:9200

serverHost: "0.0.0.0"

securityContext: {}

automountToken: false

nodeSelector:
  node-role: monitoring

resources:
  requests:
    cpu: "100m"
    memory: "300Mi"
  limits:
    cpu: "1"
    memory: "2Gi"

tolerations:
- effect: NoSchedule
  key: node-role
  operator: Equal
  value: monitoring

ingress:
  enabled: false
  apiVersion: "extensions/v1beta1"
  className: "nginx"
  pathtype: ImplementationSpecific
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
  hosts:
    - host: new-logs.sirius.online
      paths:
        - path: /
